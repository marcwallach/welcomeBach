--- welcomeBach	2020-06-16 14:34:06.992551826 -0300
+++ welcomeBach-lynx-dmenu	2020-06-16 21:40:54.815654545 -0300
@@ -1,17 +1,10 @@
-#!/usr/bin/bash
+#!/bin/bash
 
 search="$(RANDOM=$((( -1 * (20200101 - $(date '+%Y%m%d'))))) ; grep $(($RANDOM % 1129 + 1)) bwv.txt)"
 
 url="https://www.youtube.com/results?search_query=${search// /+}"
 
-links=($( curl -s "$url" | awk -F "\"" '$14 ~ /watch\?/ && length($14) == 20 {print $14}' | head -n 5))
+links=$(lynx --dump $url | awk '/watch\?.{13}$/ {print $2}' | head -n 5)
 
-echo "Today's Bach is: $search
-"
-PS3="
-Choose a link for a performance: "
-COLUMNS=1
-select link in "${links[@]}"; do
-	$BROWSER "https://www.youtube.com$link"
-  break
-done
+[ "$(printf "No\\nYes" | dmenu -i -p "Wanna listen to some Bach right now?")" = "Yes" ] &&
+	echo "${links//[[:space:]]/$'\n'}" | dmenu -l 5 -p "Today's Bach is $search" | xargs -r $BROWSER
